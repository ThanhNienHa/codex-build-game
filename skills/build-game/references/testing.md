# Testing reference

Read `docs/testing.md`. Prove rules with deterministic unit tests, adapters with contract fixtures, flows with replay tests, and boot/render behavior with a small UI smoke test. Finish with `npm run check`.
