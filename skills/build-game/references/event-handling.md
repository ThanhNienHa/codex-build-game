# Event handling reference

Read `docs/event-handling.md`. Normalize provider payloads server-side into `LiveEvent`. Design for validation, deduplication, ordering, reconnect, rate limits, burst batching, observability, and offline simulation.
